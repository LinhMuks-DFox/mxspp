# Recursive fib on the real object model (run-core): recursion, if, comparison, arithmetic.
func fib(n: int) -> int {
    if n <= 1 { return n; }
    return fib(n - 1) + fib(n - 2);
}
func main() -> int {
    println(fib(10));   # 55
    return 0;
}
