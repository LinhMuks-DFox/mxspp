# raise / exit as functions (run-core). exit(2) ends the program with code 2.
# Expected: "before" printed, then exit code 2 ("after" not reached).
func main() -> int {
    println("before");
    exit(2);
    println("after");
    return 0;
}
